import React from 'react';
import { css, styled } from '../../stitches.config';

const WaplWrap = css('div', {
    backgroundColor: 'red',
    width: '100%',
});

export const Wapl = () => {
    return <div className={WaplWrap}>WAPLL </div>;
};
